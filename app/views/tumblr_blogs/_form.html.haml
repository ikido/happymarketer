= simple_form_for @tumblr_blog, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  .form-actions
    = f.submit 'Save', :class => 'btn btn-primary'
    = link_to 'Cancel', tumblr_blogs_path, :class => 'btn'
